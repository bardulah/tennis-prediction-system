# Metabase Application Database (stores Metabase app data)
MB_DB_TYPE=postgres
MB_DB_DBNAME=your-database-name
MB_DB_PORT=5432
MB_DB_USER=your-database-user
MB_DB_PASS=your-database-password
MB_DB_HOST=your-neon-host.aws.neon.tech

# Metabase Configuration
MB_JETTY_PORT=3000

# Optional: Set admin credentials for first-time setup
# MB_ADMIN_EMAIL=admin@example.com
# MB_ADMIN_PASSWORD=changeme
# MB_ADMIN_FIRST_NAME=Admin
# MB_ADMIN_LAST_NAME=User

# Data Source Connection (Tennis Predictions Database)
# This will be configured through Metabase UI on first login
# Use the same Neon credentials above or configure a separate read-only user
