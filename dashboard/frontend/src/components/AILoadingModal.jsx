import { motion, AnimatePresence } from 'framer-motion'

export default function AILoadingModal({ isOpen, analysis, onClose }) {
  return (
    <AnimatePresence>
      {isOpen && (
        <div className="fixed inset-0 z-50 flex items-center justify-center">
          {/* Backdrop */}
          <motion.div
            initial={{ opacity: 0 }}
            animate={{ opacity: 1 }}
            exit={{ opacity: 0 }}
            className="absolute inset-0 bg-black/60 backdrop-blur"
            onClick={onClose}
          />
          
          {/* Modal */}
          <motion.div
            initial={{ opacity: 0, scale: 0.9, y: 20 }}
            animate={{ opacity: 1, scale: 1, y: 0 }}
            exit={{ opacity: 0, scale: 0.9, y: 20 }}
            className="relative w-full max-w-4xl mx-4 max-h-[80vh] overflow-hidden"
          >
            <div className="glass-panel rounded-3xl border border-slate-800/70 overflow-hidden">
              {/* Header */}
              <div className="flex items-center justify-between p-6 border-b border-slate-800/70">
                <div className="flex items-center gap-3">
                  <div className="text-2xl">🤖</div>
                  <div>
                    <h3 className="text-lg font-semibold text-slate-100">AI Analysis</h3>
                    <p className="text-sm text-slate-400">Powered by Google GenAI</p>
                  </div>
                </div>
                <button
                  onClick={onClose}
                  className="p-2 rounded-full hover:bg-slate-800/60 text-slate-400 hover:text-slate-200 transition-colors"
                >
                  ✕
                </button>
              </div>
              
              {/* Content */}
              <div className="p-6 overflow-y-auto max-h-[60vh]">
                {analysis ? (
                  <div className="prose prose-slate prose-invert max-w-none">
                    <pre className="whitespace-pre-wrap text-slate-200 font-sans leading-relaxed">
                      {analysis}
                    </pre>
                  </div>
                ) : (
                  <div className="text-center py-12">
                    <div className="relative">
                      <div className="animate-spin h-12 w-12 border-4 border-teal-400 border-t-transparent rounded-full mx-auto mb-4"></div>
                      <div className="absolute inset-0 flex items-center justify-center">
                        <div className="text-2xl">🤖</div>
                      </div>
                    </div>
                    <div className="text-lg font-medium text-slate-200 mb-2">
                      Analyzing match with AI...
                    </div>
                    <div className="text-slate-400 text-sm">
                      Fetching real-time data and generating insights
                    </div>
                  </div>
                )}
              </div>
              
              {/* Footer */}
              {analysis && (
                <div className="flex items-center justify-between p-6 border-t border-slate-800/70">
                  <div className="text-xs text-slate-500">
                    Analysis generated by AI • Use with your own research
                  </div>
                  <motion.button
                    onClick={onClose}
                    className="uiverse-button px-6 py-2"
                    whileHover={{ scale: 1.02 }}
                    whileTap={{ scale: 0.98 }}
                  >
                    Close
                  </motion.button>
                </div>
              )}
            </div>
          </motion.div>
        </div>
      )}
    </AnimatePresence>
  )
}
